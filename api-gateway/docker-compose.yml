version: "3.8"

services:
  api-gateway:
    build: .
    image: api-gateway:latest
    container_name: api-gateway
    networks:
      - patient-network
    ports:
      - "4004:4004"

networks:
  patient-network:
    external: true
